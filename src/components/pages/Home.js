import React from "react";
import "./style.css";

const Home = () => (
  <div className="home-container">
    <h1>Clicky Game!</h1>
    <h3>click on an image to earn points, but dont click on an image more than once!</h3>
  </div>
);

export default Home;
